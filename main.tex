\documentclass[12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{longtable}
\usepackage{booktabs}

\title{Rules--Driven Weekly Service Dispatch\\for New England SteamWorks}
\author{VB}
\date{}

\begin{document}

\maketitle

% ===================================================================
\section*{Problem Description}
% ===================================================================

New England SteamWorks (NES) dispatches multiple technicians with service vehicles to perform maintenance jobs across a wide geographic region every week.
The Python scheduling engine is a \textbf{draft-generation and review-support tool}: once each week it takes the eligible candidate jobs, applies written business rules and weekly exceptions, and produces a draft weekly transcription calendar that the owner (Ryan) can review and manually enter into ServiceM8.

The engine is \emph{not} a customer-booking tool, not a ServiceM8 calendar-writing tool, and not the owner of the broader Airtable\,/\,ServiceM8 invite lifecycle.
It should not auto-write the ServiceM8 calendar, directly book customers, or take over downstream invitation management.

\paragraph{Scope guardrail.}
If the solution starts to feel larger than ``produce a reliable weekly draft calendar plus the review artefacts needed to support Ryan's decision,'' it is drifting beyond the intended build.
The engine stays centred on weekly draft schedule generation, review support, and three specifically defined callable workflows (Weekly Run, Replacement Job, Replacement Route).

The weekly planning task proceeds in two deterministic phases:
\begin{enumerate}
    \item \textbf{Phase~1: Weekly Scheduling.} Apply queue-based ranking, score each candidate job, then assign jobs to (technician, vehicle, day) route slots by walking a priority--sorted list and checking eligibility and capacity rules.
    \item \textbf{Phase~2: Daily Stop Ordering.} For each day's assigned job set, arrange stops in a sensible geographic sequence using a nearest-neighbour heuristic.
\end{enumerate}

The engine must:
\begin{itemize}
    \item Serve as many high--priority jobs as possible each week, respecting all business rules;
    \item Produce compact, drivable daily routes;
    \item Make every decision explainable---traceable to a named rule and its inputs;
    \item Produce reviewable route/day proposals and a transcription calendar (not direct calendar writes);
    \item Distinguish assigned jobs, standby jobs, and anchor-hold jobs in its outputs.
\end{itemize}

\paragraph{What success looks like.}
Ryan reviews the proposed week, sees a draft calendar that makes practical sense, and can manually transcribe it into ServiceM8 without routine rebuilding.

% ===================================================================
\section*{Key Objectives (KPIs)}
% ===================================================================
\begin{itemize}
    \item \textbf{Primary goal:} Serve as many high--priority jobs as possible each week.  Priority is determined by queue placement, aging pressure, geographic quality, and seasonal ranking rules.
    \item \textbf{Secondary goals:} Keep daily workloads roughly even across technicians; keep routes geographically compact; maintain area readiness for customer communication.
    \item \textbf{Acceptance standard:} Most live weeks should be approved by Ryan with no changes.  One imperfect route does not fail the entire week, but if Ryan is routinely rebuilding, the system has not met the goal.
    \item \textbf{Non--goal for Initial Build:} Mathematical optimality.  The engine should produce \emph{good, explainable} plans---not provably optimal ones.
\end{itemize}

% ===================================================================
\section*{System Overview}
% ===================================================================

\begin{itemize}
    \item \textbf{ServiceM8} -- system of record for jobs, technicians, and operational data.
    \item \textbf{Airtable} -- scheduling rules, policy settings, review/communication layers, and the radiator-hours table.
    \item \textbf{Python Engine} -- rule evaluation, scheduling, stop ordering, output generation, and the three callable workflows (Weekly Run, Replacement Job, Replacement Route).
    \item \textbf{Make\,/\,Zapier} -- orchestration and automation between the above.
\end{itemize}

The Python engine consumes four structured input payloads---\textbf{Candidate Jobs}, \textbf{Weekly Context}, \textbf{Weekly Exceptions}, and \textbf{Lookup\,/\,Rules Data}---and produces structured outputs including route groupings, standby lists, pre-route maps, a transcription calendar, and record-based communications.
The exact transport format is flexible (CSV, JSON, or another structured payload); the engine builds to the required data contract, not one rigid file-name convention.

Every decision the engine makes can be traced to a named rule and the data that triggered it.

% ===================================================================
\section*{Queue and State Rules}
\label{sec:queues}
% ===================================================================

Queue placement is the main control layer for Initial Build.
The engine consumes queue meaning; it does not invent new queue meaning.
A job cannot be in two queues at once.

\medskip
{\scriptsize
\begin{longtable}{p{2.2cm}cp{2.5cm}p{1.5cm}p{6.5cm}}
\toprule
\textbf{Queue / State} & \textbf{Elig.?} & \textbf{Relative Treatment} & \textbf{Geo Flex} & \textbf{Notes} \\
\midrule
Urgent          & No  & Manual only             & ---          & Always excluded from weekly engine.  Includes Travel Badge / expedited / emergency / weekend service once bumped to Urgent. \\
On Hold         & No  & Manual only             & ---          & Always excluded.  No special override. \\
Priority        & Yes & Highest manual boost    & Substantial  & Ryan-only assignment by queue placement.  No separate priority-reason field required.  Can justify substantial geography weakening; may serve as anchor. \\
Accepted Quotes & Yes & Below Priority, above RS & Substantial  & Always eligible once accepted.  Treat as always Two-Man in Initial Build.  Substantial geography weakening allowed; fragile route may use anchor-hold. \\
Requested Scheduling & Yes & Fixed boost below Priority & Moderate & Ryan-only catchall boost queue; includes overbooked jobs and other jobs Ryan wants boosted.  Moderate geography weakening allowed.  May serve as anchor. \\
Scheduling Preferences & Yes & Early-pass review pool (not a score boost) & Moderate if needed & Clear preferences are hard rules; vague notes \textbf{must be flagged for Ryan and not scheduled until clarified}.  Checked after anchor/priority jobs and before normal jobs.  Detail source pending Amy. \\
Normal jobs     & Yes & Base treatment           & Normal only  & Normal geography only unless the job has crossed the $2\times$-average threshold in winter.  No special queue boost. \\
\bottomrule
\end{longtable}
}

\paragraph{Rebook rule.}
If a customer rebooks and leaves scheduling notes or preferences, the job keeps its place in line.
If the customer rebooks with no notes, the job loses its place and aging restarts.
This is separate from the broader Scheduling Preferences concept.

% ===================================================================
\section*{Vehicle and Route-Slot Model}
% ===================================================================

Current practical daily route capacity is \textbf{4 route slots}: the 3~named technicians plus Van~4 when active (addendum~\S3).
Five physical vehicles serve these four slots.

\medskip
{\small
\begin{longtable}{lp{4cm}p{5.5cm}}
\toprule
\textbf{Vehicle / Slot} & \textbf{Allowed Work} & \textbf{Restrictions} \\
\midrule
Main vehicles (V-1, V-3, V-5)  & Standard work, service calls, inspections, boiler maintenance, Accepted Quotes & Functionally equivalent; no special restrictions between the three.  Each is driven by one named technician. \\
Vehicle~2 (V-2)    & Radiator day only & \textbf{Not a separate route slot.} V-2 replaces a technician's main vehicle on radiator days; it does not add a 5th row to the transcription calendar. Must not be used as a normal standard route in Initial Build. \\
Vehicle~4 (V-4, when active) & Steam system inspections, service calls, boiler maintenance \textbf{only} & The 4th route slot (overflow); \textbf{cannot} carry Accepted Quotes, Go Backs, Estimates, helper-required, or Two-Man jobs. Controlled by the \texttt{include\_v4} flag in Weekly Context.  Not a full equivalent route slot; it is a lighter overflow route with work-type restrictions. \\
NH route           & Normal age/queue/fairness logic still applies & Default structured overnight/Two-Man; one-day NH-border exception allowed and may be one-man. \\
\bottomrule
\end{longtable}
}

\subsection*{Helper Model}
\begin{itemize}
    \item The number of helpers available is set in Weekly Context (current default: 2).
    \item The engine outputs \textbf{helper required = yes/no} per route/day; it does \emph{not} assign specific helpers by name.
    \item If any job on a route requires a helper, the entire route/day is helper-required.
    \item Once a helper is assigned to a technician, they stay together for the whole day.
    \item Boiler maintenance, service calls, and steam system inspections do not require a helper by job type.
    \item Accepted Quotes are treated as always Two-Man in Initial Build.
\end{itemize}

\subsection*{Route-Capacity Targets}
Two capacity limits apply simultaneously to each (vehicle, day) slot:
\begin{enumerate}
    \item \textbf{Physical capacity} (rule \texttt{CAPACITY\_OK}): the hard maximum stops the vehicle can physically hold. Differs by vehicle type (e.g.\ 14 for main trucks, 10 for V-4, 8 for V-2).
    \item \textbf{Seasonal booking cap} (rule \texttt{BOOKED\_CAP}): the target number of booked jobs per route, set by the current season from Weekly Context:
    \begin{itemize}
        \item \textbf{Winter:} 4~booked + 2~standby per route (possible 5th booked when backlog is high).
        \item \textbf{Summer:} 3~booked + 2~standby per route.
    \end{itemize}
\end{enumerate}
The standby pool is capped at 2~candidates per route (spec~\S5).

\subsection*{Technician and Job Parameters}

Each technician carries a skill set, a weekly availability schedule (after exceptions are applied), and a home depot / start location.

Each candidate job carries (per the Candidate Jobs field dictionary):
\begin{itemize}
    \item Job Number / Job ID (primary identifier).
    \item Service address with City, State, and geocoordinates.  Preferred source is Airtable; Python geocodes as fallback.
    \item Normalised two-digit Area Number and human-readable Area Name.
    \item Job Category / Job Type (arrives pre-standardised from ServiceM8).
    \item ServiceM8 Queue (consumed as-is; see Section~\ref{sec:queues}).
    \item Required Job Hours when present; else fallback rules apply (Section~\ref{sec:categories}).
    \item Radiator Count and Refinisher Location (radiator jobs only).
    \item Created Date or Computed Job Age (days).
    \item $2\times$ Average Wait Flag (Airtable-managed aging signal).
    \item Rebook Count With Notes / Without Notes (supports rebook-age rule).
    \item Scheduling Preference Detail (when available; source pending).
\end{itemize}

\subsection*{Time and Distance}
\begin{itemize}
    \item Drive times are derived fresh at runtime by the engine using a routing service (e.g.\ Google Maps), not simple distance math.
    \item Geocodes: preferred Airtable-owned; Python fallback if Airtable cannot supply reliably.  If Python cannot geocode an address, the job is skipped with a communication record.
    \item Each vehicle has a maximum-stops capacity.
    \item Each route has a maximum daily duration (minutes).
    \item Technicians leave the shop at 7:00\,a.m.
\end{itemize}

\subsection*{Weekly Exceptions}
Before route construction the engine applies weekly exceptions (the authoritative source of within-week availability reduction):
\begin{itemize}
    \item Full technician-day blocks (Initial Build supports full-day blocks only).
    \item Vehicle unavailability.
    \item Pre-booked capacity that consumes partial route slots.
\end{itemize}
These reduce effective weekly capacity before the scheduling layer begins.

% ===================================================================
\section*{Seasonal Scoring and Ranking}
\label{sec:scoring_model}
% ===================================================================

The engine uses a short \textbf{decision-ladder} ranking rather than a large point system.
A job cannot be in two queues at once.

\paragraph{Scheduling Preferences.}
Scheduling Preferences are handled separately as an early-pass constraint/review layer and are \textbf{not part of queue scoring}.
Jobs in this queue are scored at the same tier as Normal jobs; their preferences are applied as hard constraints (clear preferences) or flagged for Ryan (vague notes), not via a tier boost.

\paragraph{Winter ranking order.}
\begin{enumerate}
    \item Priority jobs first.
    \item Normal jobs that have crossed the $2\times$-average-wait threshold.
    \item Accepted Quotes and Requested Scheduling (equal tier; better route wins).
    \item Normal jobs last (including Scheduling Preferences jobs that passed preference checks).
\end{enumerate}
Within winter ranking, aging matters and older jobs are preferred, but route/geography must still make reasonable sense.
A $2\times$-average job does not justify absurd isolation or nonsense geography.

\paragraph{Summer ranking order.}
\begin{enumerate}
    \item Priority jobs first.
    \item Accepted Quotes and Requested Scheduling (equal tier; route quality decides).
    \item Normal jobs last (including Scheduling Preferences jobs that passed preference checks).
\end{enumerate}
In summer, aging is \emph{not} an active scoring factor for normal route selection.

% ===================================================================
\section*{Geographic Flexibility and Route-Shape Rules}
\label{sec:geography}
% ===================================================================

The engine judges geography using both local proximity and full route shape, with \textbf{full route shape mattering most}.
Providence shop is an explicit routing factor in Initial Build.

\begin{itemize}
    \item Acceptable route shapes: clusters, sensible lines/corridors, and Providence-assisted shapes.
    \item On-the-way jobs not near the cluster may be included only as \textbf{first-only} or \textbf{last-only} jobs.
    \item First-only / last-only always applies; queue status does not override it.
    \item Line/corridor routes are acceptable only when they flow sensibly without zigzagging.
\end{itemize}

\paragraph{Implementation status.}
The current code uses \textbf{depot-to-job haversine distance} as a proxy for geographic quality.
Full route-shape analysis (cluster tightness, corridor flow, zigzag detection), per-queue flexibility differentiation, and Providence-shop-as-routing-factor are spec requirements that map to the deferred \texttt{ROUTE\_SHAPE\_OK} and \texttt{POSITION\_OK} eligibility rules.
The geography-flexibility table below documents the \emph{target} behaviour; the code applies uniform proximity today.

\paragraph{Geography flexibility by queue / trigger.}

\medskip
{\small
\begin{longtable}{lp{3.5cm}p{6cm}}
\toprule
\textbf{Job class / trigger} & \textbf{Geography flexibility} & \textbf{Practical meaning} \\
\midrule
Normal jobs           & Normal geography only & Use sensible cluster or sensible line/corridor; do not weaken just because capacity is tight. \\
Requested Scheduling  & Moderate & Can flex beyond normal geography, but route still has to make practical sense. \\
Priority              & Substantial & May justify substantially weaker geography and even fragile routes. \\
Accepted Quotes       & Substantial & Long-duration jobs make geography less important; fragile route allowed with anchor-hold. \\
$2\times$-average Normal (winter override) & Can outrank AQ / RS in winter & Route must still make reasonable sense.  Do not use this override for absurd isolation or nonsense geography.  Trigger source still unresolved. \\
Scheduling Preferences & Moderate if needed & Hard preference constraints still control eligibility; do not force a fragile/underfilled day just because preference matched. \\
\bottomrule
\end{longtable}
}

\paragraph{Seasonal emphasis.}
Summer emphasises best/most efficient geography.
Winter gives aging more weight, so clusters and sensible lines are both acceptable based on aging pressure.
When winter aging and geography are in tension, prefer the older job even if geography is somewhat worse.

\paragraph{Winter emergency coverage.}
In winter, always aim to keep one vehicle in the Boston metro area (Areas 9, 10, 11, 14 are interchangeable for Boston coverage).
Providence coverage is desirable but lower priority because all vehicles return toward Providence anyway.

% ===================================================================
\section*{Job-Category Planned Hours}
\label{sec:categories}
% ===================================================================

Job categories arrive pre-standardised from ServiceM8; the engine does not normalise category names.
What matters for scheduling is the planned-hours source for each category.

\medskip
{\small
\begin{longtable}{lp{4cm}p{4.5cm}}
\toprule
\textbf{Job Category} & \textbf{Planned-Hours Source} & \textbf{Initial Build Handling} \\
\midrule
Boiler Maintenance                & Fixed default time  & 1 hour \\
Castrads New Radiator(s)          & Required Job Hours for Scheduling  & Use the Required Job Hours for Scheduling field \\
Estimate                          & Fixed default time  & 1 hour \\
Go Back                           & Fixed default time  & 1 hour \\
Insulation                        & Required Job Hours for Scheduling  & Use the Required Job Hours for Scheduling field \\
New Boiler Visit                  & Fixed default time  & 1 hour \\
New Equipment Installation Other Than Boiler & Required Job Hours for Scheduling & Use the Required Job Hours for Scheduling field \\
Radiator Pick-Up                  & Fixed default time  & 1 hour \\
Radiator Refinishing              & Airtable radiator-hours table & Read active Airtable values \\
Radiator Replace \& Refinish     & Airtable radiator-hours table & Read active Airtable values \\
Radiator Replacement              & Airtable radiator-hours table & Read active Airtable values \\
Radiator Service                  & Fixed default time  & 1 hour \\
Service Call                      & Fixed default time  & 1 hour \\
Steam System Inspection           & Fixed default time  & 1 hour \\
Trap Service                      & Required Job Hours for Scheduling  & Use the Required Job Hours for Scheduling field \\
Vent / Trap / Repair / Piping Work & Required Job Hours for Scheduling  & Use the Required Job Hours for Scheduling field \\
Accepted Quotes / consult-style quoted work & Required Job Hours for Scheduling if present; otherwise formula fallback & Fallback: total\_job\_amount $\div$ 350, rounded up to next half hour \\
\bottomrule
\end{longtable}
}

The radiator-hours formula lives in Airtable, not in hard-coded Python logic.
The engine must consult the Airtable radiator-hours table for active step values when estimating radiator job hours.

\paragraph{Missing-hours fallback.}
When a job belongs to a ``Required Job Hours for Scheduling'' category but the field is empty or zero, the engine falls back to 1.0~hour and surfaces a \textbf{Pre-Route Communication} warning (severity~=~Warning, type~=~``Missing Planned Hours'') identifying the job and category.

Vehicle eligibility, technician eligibility, and similar assignment restrictions are not defined by the category-time table (addendum~\S3).
They are supplied through Airtable-managed lookup/rules inputs and weekly context data rather than hard-coded in the category-time section.

% ===================================================================
\section*{Input Data}
% ===================================================================

The engine assumes the candidate pool has already been pre-filtered upstream to only eligible Work Order jobs.
Jobs already scheduled, cancelled, already in Airtable workflow, or manually handled as Urgent are excluded before the engine runs.

\subsection*{Data Buckets Overview}

The seven semantic data buckets (four inputs, three outputs) are summarised below.
Exact Airtable naming can be finalised at implementation lock; what matters is the semantic contract.

\medskip
{\small
\begin{longtable}{lp{5cm}p{5cm}}
\toprule
\textbf{Bucket} & \textbf{What it means} & \textbf{Minimum required content} \\
\midrule
Candidate Jobs & Weekly pool of eligible jobs after upstream exclusions. & Job Number; dates/age; address fields; area; category; queue; hours; radiator/refinisher; aging flags; rebook counts; scheduling preference; total job amount (AQ). One record per job. \\
Weekly Context & Week-level settings for the run. & Week Of; Season; Include V-4 This Week; holiday list; linked Weekly Exceptions. One record per week. \\
Weekly Exceptions & Week-specific capacity reductions. & Technician-day blocks; vehicle unavailability; pre-booked capacity (if represented). Authoritative source of within-week availability reduction. \\
Lookup / Rules Data & Reference tables and Airtable-managed rule inputs. & Technician/vehicle/area lookups; adjacency/grouping rules; restriction flags; aging/fairness flags; route-eligibility tables. Not a live job list. \\
Schedule Week Outputs & Owner-review outputs from Weekly Run. & Proposed week; route grouping; route flags; pre-route maps; transcription calendar; Pre-Route Communications. \\
Pre-Route Communications & Record-based AI outputs before ServiceM8 transcription. & Issue type; severity; message; schedule-week link; optional job/route context. One row per issue. \\
Route Communications & Record-based AI outputs after sync in route/day/technician interface. & Issue type; severity; message; schedule-week link; required route-day and route-slot context; optional job link. One row per issue. \\
\bottomrule
\end{longtable}
}

\subsection*{Candidate Jobs (Input Payload~1)}

One record per candidate job.  Required fields:

\medskip
{\small
\begin{longtable}{lclp{4cm}}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Req?} & \textbf{Notes} \\
\midrule
Job Number / Job ID      & Text    & Required  & Primary identifier \\
Created Date             & Date    & Required$^*$  & Either Created Date or Computed Job Age \\
Computed Job Age         & Number  & Required$^*$  & Age in days (Airtable may compute upstream) \\
Service Address          & Text    & Required  & Street address for geocoding \\
City                     & Text    & Required  & City component \\
State                    & Text    & Required  & State component \\
Normalised Area Number   & Text    & Required  & Two-digit area identifier \\
Area Name                & Text    & Required  & Human-readable area label \\
Job Category / Type      & Text    & Required  & Arrives pre-standardised \\
ServiceM8 Queue          & Text    & Required  & Queue meaning consumed by Python \\
Required Job Hours       & Number  & Optional  & Used whenever present \\
Radiator Count           & Number  & Optional  & Radiator jobs only \\
Refinisher Location      & Text    & Optional  & Radiator jobs only \\
2$\times$ Avg Wait Flag  & Boolean & Preferred & Airtable-managed aging signal \\
Rebook Count (with notes)  & Number & Optional & Supports rebook-age rule \\
Rebook Count (no notes)    & Number & Optional & Supports rebook-age rule \\
Scheduling Pref Detail   & Long text & Optional & Source pending Amy \\
Total Job Amount         & Number & Optional & Used for Accepted Quotes fallback (amount $\div$ 350, rounded up to next half hour) \\
\bottomrule
\end{longtable}
}

\subsection*{Weekly Context (Input Payload~2)}

One record per schedule week.

\medskip
{\small
\begin{longtable}{lclp{4.5cm}}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Req?} & \textbf{Notes} \\
\midrule
Week Of            & Date     & Required  & Monday date for the target scheduling week \\
Season             & Text     & Required  & ``Winter'' or ``Summer''; controls seasonal decision-ladder logic \\
Include V-4 This Week & Boolean & Required & Turns the overflow route slot on or off \\
Holiday List       & Long text / linked & Optional & Only if a holiday affects the week \\
Weekly Exceptions  & Linked records & Required & Authoritative source of within-week availability reduction \\
Helpers Available  & Number   & Optional  & Helper-day capacity toggle (default 2) \\
\bottomrule
\end{longtable}
}

\subsection*{Weekly Exceptions (Input Payload~3)}

The authoritative source of within-week availability reduction.  Initial Build assumes full technician-day blocks only.

\medskip
{\small
\begin{longtable}{lclp{4cm}}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Req?} & \textbf{Notes} \\
\midrule
Exception ID             & Text   & Required & Unique identifier \\
Week Of                  & Date   & Required & Links to scheduling week \\
Technician / Route Slot  & Text   & Required & Which slot is affected \\
Exception Type           & Text   & Required & Full technician-day block (Initial Build) \\
Affected Day             & Text   & Required & Day of week impacted \\
Notes                    & Long text & Optional & Plain-language context \\
\bottomrule
\end{longtable}
}

\subsection*{Lookup / Rules Data (Input Payload~4)}

Reference tables and Airtable-managed rule inputs (not a live job list):

\medskip
{\small
\begin{longtable}{lclp{4.5cm}}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Req?} & \textbf{Notes} \\
\midrule
Lookup Item      & Text      & Required & Human-readable name of the reference rule \\
Lookup Type      & Text      & Required & E.g.\ ``Vehicle restriction'', ``Area rule'', ``Grouping rule'' \\
Rule / Value     & Long text & Required & Reference value consumed by Python or surfaced to Amy \\
Active?          & Boolean   & Required & Allows Airtable-managed activation / deactivation \\
\bottomrule
\end{longtable}
}

These tables cover technician / vehicle / area lookup data; area naming, adjacency, and grouping rules; Airtable-managed restriction flags and aging/fairness flags; and route-eligibility tables Amy manages in Airtable.

\subsection*{Validation and Fallback Behaviour}
\begin{itemize}
    \item If a job is missing geocodes, Python geocodes it as fallback.  If that also fails, the job is skipped.
    \item If Required Job Hours is missing, Python uses the defined fallback rules (Section~\ref{sec:categories}) rather than skipping the job.
    \item If another required non-hour field is missing, the job is skipped.
    \item Skipped jobs are surfaced through the communication flow (Job~\# and what needs fixing). No standalone validation report is needed.
\end{itemize}

\subsection*{Duplicate--Address Handling}
\label{sec:duplicates}

Duplicate policing primarily happens upstream in Airtable before Python scheduling begins.
Python is \textbf{not} expected to be the main duplicate-detection system for Initial Build.
If a suspected duplicate still slips through and materially affects route construction, Python surfaces it through Pre-Route or Route Communications.
Where duplicate review is still needed in Python fallback scenarios, the flag should be minimal: \textbf{Job~\# and what needs fixing}.

\paragraph{Radiator replacement slave job exception.}
Multiple radiator replacement slave jobs at 15 Monticello, Providence must \textbf{never} be treated as duplicates.
That address is the storage facility and multiple same-day pickup jobs there are expected.
The engine continues to flag probable duplicates for Ryan review when duplicate jobs are detected by matching property address, except for the radiator slave job exception above.

\subsection*{Implementation File Mapping}
The four abstract payloads plus two supporting resources map to six CSV files
in the \texttt{data/} folder:

\medskip
{\small
\begin{tabular}{lll}
\toprule
\textbf{Payload / Resource} & \textbf{File} & \textbf{Rows} \\
\midrule
Candidate Jobs (1)  & \texttt{example\_jobs.csv}                & One per job \\
Weekly Context (2)   & \texttt{example\_weekly\_context.csv}     & Single row per week \\
Weekly Exceptions (3) & \texttt{example\_weekly\_exceptions.csv} & One per exception \\
Lookup / Rules (4)   & \texttt{example\_area\_rules.csv}         & One per rule \\
Technicians          & \texttt{example\_technicians.csv}         & One per technician \\
Vehicles             & \texttt{example\_vehicles.csv}            & One per vehicle \\
\bottomrule
\end{tabular}
}

\medskip
The standalone script \texttt{weekly\_data.py} can generate these files
(\texttt{-{}-generate}), load and validate them (\texttt{-{}-load}), or pull
live jobs from ServiceM8 (\texttt{-{}-servicem8}).  The core library
\texttt{nes\_dispatch/data/loaders.py} reads the same files into domain
dataclasses.

% ===================================================================
\section*{Planning Pipeline}
\label{sec:pipeline}
% ===================================================================

The weekly planning pipeline runs the following stages in order.
Every stage is a deterministic function of its inputs; there is no randomness and no external solver.

\begin{enumerate}
    \item \textbf{Load \& Validate} -- Import the four input payloads.  Run validation checks (Section~\ref{sec:validation}).  Skip jobs with unfixable missing data; surface skipped jobs through communications.
    \item \textbf{Apply Weekly Exceptions} -- Subtract technician-day blocks and vehicle unavailability from each technician's and vehicle's available days.
    \item \textbf{Reserve Special Routes} -- Place Radiator Runs (require V-2), NH Overnight Runs, and helper-needed routes \emph{before} normal filling.
    \item \textbf{Phase~1: Queue-Based Scoring \& Scheduling (Section~\ref{sec:scheduling})} -- Apply the seasonal decision-ladder ranking; walk the sorted list and assign each job to the best eligible (technician, vehicle, day) slot.
    \item \textbf{Phase~2: Order Stops (Section~\ref{sec:routing})} -- For each day's assigned job set, arrange stops in geographic order using a nearest-neighbour heuristic.  Verify time and distance feasibility.
    \item \textbf{Select Standby Jobs} -- Choose the two lowest-priority acceptable candidates per route as standby.
    \item \textbf{Compute Area Readiness} -- Derive area communication layer (\texttt{Good}\,/\,\texttt{Moderate}\,/\,\texttt{Lean}).  Ownership note: if Airtable can calculate it, Airtable should.
    \item \textbf{Generate Communications} -- Emit Pre-Route Communications for week-build issues.
    \item \textbf{Render Outputs} -- Route maps, flags, transcription calendar, reason codes.  Exclusion reasons are surfaced as Pre-Route Communications (spec~\S14.2).
\end{enumerate}

% ===================================================================
\section*{Phase~1: Queue-Based Weekly Scheduling}
\label{sec:scheduling}
% ===================================================================

The scheduling phase decides \emph{which} jobs to serve and \emph{who does them, with which vehicle, on which day}.
It uses the seasonal decision-ladder ranking (Section~\ref{sec:scoring_model}) followed by a deterministic assign loop.

\subsection*{Step 1 -- Queue-Based Ranking}

Jobs in the weekly candidate pool are ordered by the decision-ladder model rather than a single composite score.
The engine first groups jobs by queue tier (see Section~\ref{sec:queues}), then applies seasonal ranking:

\paragraph{Winter.}
\begin{enumerate}
    \item Priority jobs (highest).
    \item Normal jobs that have crossed the $2\times$-average-wait threshold.
    \item Accepted Quotes and Requested Scheduling (equal tier; the job that produces the better route wins).
    \item Normal jobs (lowest).
\end{enumerate}
Within each tier, older jobs are preferred, but geography must still make reasonable sense.

\paragraph{Summer.}
\begin{enumerate}
    \item Priority jobs (highest).
    \item Accepted Quotes and Requested Scheduling (equal tier; route quality decides).
    \item Normal jobs (lowest).
\end{enumerate}
Aging is not an active tiebreaker for normal route selection in summer.

\paragraph{Scheduling Preferences pass.}
Scheduling Preferences are handled as an early-pass constraint/review layer \emph{after} anchor/priority jobs and \emph{before} normal jobs.
Clear preferences (e.g.\ ``Tuesday or Thursday mornings'') are hard rules.
Vague or unclear notes are flagged for Ryan and not scheduled until clarified.

\subsection*{Step 2 -- Eligibility Check}

For each candidate assignment of a job to a technician, vehicle, and day, the engine evaluates a named eligibility checklist.
The assignment is \textbf{eligible} only if \emph{every} rule passes:

\medskip
{\small
\begin{longtable}{lp{9.5cm}}
\toprule
\textbf{Rule name} & \textbf{Condition} \\
\midrule
\texttt{TECH\_AVAILABLE}     & Technician is available on the planned day (after weekly exceptions). \\
\texttt{VEH\_AVAILABLE}      & Vehicle is available on the planned day (after weekly exceptions). \\
\texttt{SKILL\_MATCH}        & Technician skills cover the job's requirements. \\
\texttt{VEH\_CAPABILITY}     & Vehicle capability tags cover the job's requirements. \\
\texttt{VEH\_WORK\_ELIGIBLE} & Vehicle is allowed to carry the job's work type (e.g.\ V-4 cannot carry Accepted Quotes or helper-required routes). \\
\texttt{WITHIN\_RADIUS}      & Haversine distance from technician depot to job does not exceed the cluster radius (default 30\,km). \\
\texttt{NOT\_PROHIBITED}     & (technician, job) pair is not in the prohibited-pairs set. \\
\texttt{CAPACITY\_OK}        & Adding this job would not exceed the vehicle's physical stop capacity. \\
\texttt{BOOKED\_CAP}         & Seasonal booking target not exceeded (winter: 4, summer: 3, from Weekly Context). \\
\texttt{TIME\_OK}            & Adding this job's planned hours would not push the day's total past the daily time budget, accounting for travel time. \\
\texttt{ONE\_VEH\_PER\_TECH} & Technician has not already been assigned a different vehicle on this day. \\
\texttt{ONE\_TECH\_PER\_VEH} & Vehicle has not already been assigned to a different technician on this day. \\
\bottomrule
\end{longtable}
}

If every rule passes, the assignment is feasible.  If any rule fails, the engine records which rule failed and moves to the next candidate slot.

\subsection*{Step 3 -- Slot Selection (Best-Fit Assignment)}

For each job (in ranked order), the engine considers all eligible (technician, vehicle, day) slots and picks the \textbf{best fit}:

\begin{enumerate}
    \item \textbf{Closest depot.} Prefer the slot whose technician depot is nearest to the job.  This serves as a proxy for route-shape quality until the full \texttt{ROUTE\_SHAPE\_OK} rule is implemented; see Deferred Items.
    \item \textbf{Lightest day.} Among comparable proximity, prefer the day with the fewest stops already assigned.
    \item \textbf{Geography flexibility.} Apply the queue-specific flexibility level (see Section~\ref{sec:geography}): Normal jobs get normal geography only; Priority and Accepted Quotes allow substantial weakening; Requested Scheduling allows moderate weakening.  \emph{Note:} the current code applies uniform proximity for all queues; per-queue flexibility differentiation is a planned enhancement.
\end{enumerate}

If no eligible slot exists, the job is recorded with the first failing rule as the reason code.

\subsection*{Step 4 -- Helper Flagging (Two-Man Jobs)}

For every helper-needed job or any Accepted Quote (treated as always Two-Man), the engine marks the entire route/day as \textbf{helper-required = yes}.
The engine does \emph{not} assign specific helpers by name---it only outputs the flag.
Once a helper is assigned to a technician, they stay together for the whole day.

\subsection*{Step 5 -- Workload Review}

After all jobs are assigned, the engine checks workload balance:
\begin{itemize}
    \item Compute each technician's total weekly service time.
    \item If any technician exceeds 90\,\% of their available hours, emit a \texttt{TECH\_OVERLOAD} flag.
    \item If any vehicle is at 100\,\% capacity on 4+ days, emit a \texttt{VEH\_BOTTLENECK} flag.
\end{itemize}

No jobs are moved or swapped at this stage---the flags exist so Ryan can decide whether the imbalance is acceptable.

\subsection*{Phase~1 Summary}

The scheduling loop works as follows:
\begin{enumerate}
    \item Score all candidate jobs using the seasonal decision-ladder ranking.
    \item Process Scheduling Preferences as early-pass constraints.
    \item For each job in ranked order:
    \begin{itemize}
        \item Check all eligible (technician, vehicle, day) slots against the eligibility rules.
        \item If an eligible slot exists, assign the job to the best-fit slot and update capacity trackers.
        \item If the job is helper-needed or an Accepted Quote, mark the route/day as helper-required.
        \item If no eligible slot exists, record the exclusion with the first failing rule.
    \end{itemize}
    \item Select 2 standby jobs per route (lowest-priority acceptable candidates).
    \item Run workload-review checks and emit flags.
\end{enumerate}

Every assignment is explainable: ``job X scored in tier Y; slot (technician, vehicle, day) was chosen because it was the closest eligible depot on the lightest day.''
The algorithm is deterministic---same inputs always produce the same plan.

% ===================================================================
\section*{Phase~2: Stop Ordering (Nearest--Neighbour Routing)}
\label{sec:routing}
% ===================================================================

After Phase~1 assigns jobs to (technician, vehicle, day) bundles, Phase~2 arranges the stops within each day into a drivable geographic sequence.

\subsection*{Step 1 -- Build the Day's Stop List}

For each (technician, vehicle, day) bundle, collect the assigned jobs.
The depot (technician start location) is the first node.

\subsection*{Step 2 -- Nearest--Neighbour Sequencing}

Starting from the depot, repeatedly visit the closest unvisited stop until all stops have been visited, then return to the depot.

For typical NES clusters (up to 14 stops per route), this runs instantaneously.
Initial Build uses Haversine (straight-line) distances for stop \emph{sequencing}.
Note: the spec (\S3) requires drive-time calculations from a real routing service (e.g.\ Google Maps) for \emph{travel-time budgeting}; the Haversine here is the sequencing heuristic only.

\subsection*{Step 3 -- Feasibility Verification}

After sequencing, the engine walks the route and checks:

\medskip
{\small
\begin{longtable}{lp{9cm}}
\toprule
\textbf{Check} & \textbf{Action if violated} \\
\midrule
Total travel + service time exceeds the daily time budget & Drop the last-added job from the route; re-check.  Dropped job gets reason code \texttt{ROUTE\_DROP}. \\
Number of stops exceeds vehicle capacity & Should not happen (enforced in Phase~1), but drop excess if it does. \\
Inter-stop distance exceeds the inter-stop limit & Flag the arc as \texttt{CROSS\_AREA}; do not drop (guidance, not hard wall). \\
A stop falls outside the cluster radius of the depot & Should not happen (eligibility enforced), but flag \texttt{GEOCODE\_OOB} if violated. \\
\bottomrule
\end{longtable}
}

If any job is dropped, the engine re-runs the nearest-neighbour on the reduced set so the sequence stays sensible.

% ===================================================================
\section*{Hard Constraints Summary}
% ===================================================================

The following rules are enforced throughout the pipeline.  None may be relaxed; every violation either blocks the assignment or triggers a job exclusion with a reason code.
The engine evaluates \textbf{12 named eligibility rules} for every candidate (job, technician, vehicle, day) slot:

\medskip
{\small
\begin{longtable}{lp{9cm}}
\toprule
\textbf{Rule Name} & \textbf{Description} \\
\midrule
\texttt{TECH\_AVAILABLE}    & Technician must be available on the planned day (after weekly exceptions). \\
\texttt{VEH\_AVAILABLE}     & Vehicle must be available on the planned day (after weekly exceptions). \\
\texttt{SKILL\_MATCH}       & Technician skills must cover the job's requirements. \\
\texttt{VEH\_CAPABILITY}    & Vehicle capability tags must cover the job's requirements. \\
\texttt{VEH\_WORK\_ELIGIBLE}& V-4 may carry \emph{only} Steam System Inspections, Service Calls, and Boiler Maintenance. Main vehicles have no category restriction. \\
\texttt{WITHIN\_RADIUS}     & Haversine distance from technician depot to job must not exceed the cluster radius (default 30\,km). \\
\texttt{NOT\_PROHIBITED}    & (technician, job) pair must not be in the prohibited-pairs set. \\
\texttt{CAPACITY\_OK}       & Physical vehicle capacity must not be exceeded for the (vehicle, day) slot. \\
\texttt{BOOKED\_CAP}        & Seasonal booking target must not be exceeded (winter: 4, summer: 3, from Weekly Context). \\
\texttt{TIME\_OK}           & Daily service-time budget must not be exceeded (Phase~1 fraction of the daily time budget). \\
\texttt{ONE\_VEH\_PER\_TECH}& Each technician uses at most one vehicle per day. \\
\texttt{ONE\_TECH\_PER\_VEH}& Each vehicle is assigned to at most one technician per day. \\
\bottomrule
\end{longtable}
}

\noindent Additional structural constraints (enforced by the pipeline but not as per-slot eligibility checks):
\begin{itemize}
    \item Helper-required routes must be flagged; once a helper is on a route, they stay for the whole day.
    \item No job is assigned more than once.
    \item V-2 is used for radiator days only.
    \item Standby pool is capped at 2~candidates per route.
\end{itemize}

\noindent \textbf{Deferred:} \texttt{ROUTE\_SHAPE\_OK} (geographic-shape fit) and \texttt{POSITION\_OK} (first-only/last-only) are spec requirements not yet implemented as eligibility rules; they are listed in Deferred Items.

% ===================================================================
\section*{Special Route Types}
% ===================================================================

Special routes are planned \emph{before} normal route filling to reserve capacity:

\begin{enumerate}
    \item \textbf{Radiator Runs} -- Require Vehicle~2 (V-2).  V-2 must not be used as a normal standard route in Initial Build.  The Airtable radiator-hours table supplies planned-hours values.
    \item \textbf{NH Overnight Runs} -- Default structured overnight/Two-Man route.  Normal age/queue/fairness logic still applies.  A one-day NH-border exception is allowed and may be one-man.
    \item \textbf{Two-Man / Helper Routes} -- Jobs flagged as helper-required, plus all Accepted Quotes (treated as always Two-Man in Initial Build).  The entire route/day becomes helper-required if any job on it needs a helper.
\end{enumerate}

After special routes consume capacity, the remaining capacity feeds normal scheduling.

% ===================================================================
\section*{Anchor, Standby, Replacement, and Day-Recovery Logic}
\label{sec:standby}
% ===================================================================

\subsection*{Standby Job Selection}

After route construction, the engine selects two standby jobs per route---the lowest-priority jobs among otherwise acceptable route candidates.
Standby jobs must be distinguished from assigned jobs in the engine's outputs; each assignment carries an explicit \texttt{role} tag (\texttt{assigned}, \texttt{standby}, or \texttt{anchor\_hold}).

If enough valid standby jobs do not exist, the route normally should not run unless work is thin and Ryan is expected to work manual adjustments afterward.
The engine surfaces this condition through a \texttt{WEAK\_STANDBY} review flag (severity WARN) on any route with fewer than two standby candidates.

\subsection*{Anchor-Hold Logic}

For fragile routes---typically those built around long-duration Accepted Quotes, Priority jobs, or Requested Scheduling jobs with weak geography---the engine may designate an \textbf{anchor-hold job}: a high-value job that justifies the route's existence even if the supporting geography is thin.
Per the queue treatment table (Section~\ref{sec:queues}): Priority \emph{may serve as anchor}; Accepted Quotes may use anchor-hold on fragile routes; Requested Scheduling \emph{may serve as anchor}.
Anchor-hold should generally \emph{not} be used in Replacement Route (adds too much time), except when jobs are thin and it is the only workable option.

\paragraph{Implementation status.}
Anchor-hold designation is not yet implemented in code.
The standby-selection module identifies standby candidates, but the anchor-hold flag (marking a job as the route's justification) is a planned enhancement.

\subsection*{Day-Damage Recovery (Ryan-Initiated)}

The engine supports two Ryan-initiated day-recovery workflows in Initial Build.
These are \emph{not} automatic background behaviours; they are triggered by Ryan from the post-sync single-route / single-day / single-technician view.

\paragraph{Replacement Job.}
Launched when a mostly intact route loses one job.
Replacement Job is launched from the post-sync single-route / single-day / single-technician view.
If easiest to build, Airtable may send the whole route view.
The engine uses that context---current jobs, accepted jobs, and accepted booking windows---when searching for a replacement.

\begin{itemize}
    \item Return up to two good replacement candidates when they exist, unranked (both may be invited).
    \item If only one good candidate exists, return only that candidate.
    \item If no good candidate exists, return the best available candidate marked as ``outside normal parameters.''
    \item An on-the-way first-only / last-only candidate is valid only if the already accepted booking windows leave a workable first or last slot.  If that slot is not available, the candidate must be rejected and the engine should keep searching.
\end{itemize}

\paragraph{Replacement Route (Blow-Up / Rebuild).}
Launched when a day must be entirely rebuilt for the same technician/route.
The rebuilt route includes standby jobs like a normal new route and actively reconsiders the weakened day rather than just swapping one job.

If a rebuilt route is workable but weak, the engine still emits the best workable result and surfaces the warning through Route Communications rather than suppressing the route.

\paragraph{Implementation status.}
Replacement Job and Replacement Route are defined as callable workflows (Section~\ref{sec:workflows}) but are not yet implemented in code.
The Weekly Run workflow (score-and-assign pipeline) is fully implemented; the two recovery workflows are planned enhancements that will reuse the same eligibility, scoring, and slot-selection modules.

% ===================================================================
\section*{Booking-Window and Feasibility Logic}
\label{sec:windows}
% ===================================================================

The engine is not responsible for full downstream invitation management, but it must output route-position constraints and basic timing information that affects invitation choice.

\subsection*{Window Rules}

Technicians depart the shop at 7:00\,a.m.  The \emph{estimated arrival} at the first stop is 7:00 plus the travel time from the shop to that stop's address (computed via the routing service).  This arrival time determines the first window:

\medskip
{\small
\begin{tabular}{llp{6cm}}
\toprule
\textbf{Slot} & \textbf{Window} & \textbf{Rule / pairing} \\
\midrule
First stop (early)  & 7--8   & Used when estimated arrival is before 8:00.  Implies \textbf{8--11} as the second window. \\
First stop (late)   & 8--9   & Used when estimated arrival is 8:00 or later.  Implies \textbf{10--1} as the second window. \\
Third stop          & 11--2  & Fixed sequence after first two windows. \\
Fourth stop         & 1--4   & Fixed sequence. \\
Optional fifth stop & 2--5   & Used only when a fifth booked job is added. \\
\bottomrule
\end{tabular}
}

\medskip
The engine must output first-only and last-only flags when route shape requires them.

\subsection*{Feasibility / Conflict Flagging}

Initial Build supports practical feasibility flagging:
\begin{itemize}
    \item Detect obviously impossible route/window combinations.
    \item Flag likely renegotiation needs.
    \item Identify when a route probably cannot be served with the currently accepted windows.
\end{itemize}
The engine should \emph{not} become a full invitation-window planner.

\subsection*{Related Sub-Topics}

The consolidated spec (\S8) groups two additional sub-topics under booking-window logic:
\begin{itemize}
    \item \textbf{Radiator-hour source and radiator-route rules} (\S8.1) -- covered in Section~\ref{sec:categories} (Job-Category Planned Hours) and the radiator-hours notes in Deferred Items.
    \item \textbf{Duplicate-job handling} (\S8.2) -- covered in Section~\ref{sec:duplicates} (Duplicate--Address Handling).
\end{itemize}

\paragraph{Implementation status.}
Booking-window assignment and feasibility flagging are not yet implemented in code.
The current pipeline enforces time-budget constraints (\texttt{TIME\_OK}) but does not assign specific window slots (7--8, 8--9, etc.) or detect window conflicts.
This is a planned enhancement; the data model, communication records, and acceptance criteria are defined above.

% ===================================================================
\section*{Area Readiness}
% ===================================================================

The engine computes an area-readiness indicator for each geographic area: \texttt{Good}, \texttt{Moderate}, or \texttt{Lean}, based on the density and quality of planned activity in that area.
This is a communication layer for customer-facing scheduling messages and does not drive route construction.

\paragraph{Ownership note.}
Area Readiness remains a grey area.
Current best assumption: if Airtable can calculate it, Airtable should calculate/store/display it.
The engine does not need to own Area Readiness calculation in Initial Build unless later directed otherwise.

% ===================================================================
\section*{Communication Model}
\label{sec:comms}
% ===================================================================

Initial Build uses a minimal, record-based communication model (one Airtable row per issue) rather than open chat.
This makes issues easier to scan, filter, resolve, and tie back to one route or one job when needed (addendum~\S4).
Ryan-to-AI actions remain button-based only: Run AI Analysis, Approve, Reject\,/\,Re-run, Replacement Job, and Replacement Route.

\subsection*{Pre-Route Communications}

Used \emph{before} ServiceM8 transcription for week-build issues such as skipped jobs needing fixes, weak-route notes, duplicate review, clarification requests, or any other items the engine needs to tell Ryan before transcription (spec~\S10, addendum~\S4.1).
Each record contains:

\medskip
{\small
\begin{longtable}{lclp{2.8cm}p{3.2cm}}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Req?} & \textbf{Example} & \textbf{Notes} \\
\midrule
Communication ID   & Text / autonumber & Required & COM-00127 & Unique row identifier \\
Communication Type & Single select    & Required & Skipped Job & Examples differ slightly by table \\
Severity           & Single select    & Required & Warning & Info / Warning / Action Needed \\
Schedule Week      & Linked record    & Required & Week of 2026-04-06 & Parent review record \\
Message            & Long text & Required & Job 10452 skipped: missing geocode.\ Fix address and rerun. & Human-readable message Ryan can scan quickly \\
Resolved           & Checkbox  & Required & FALSE & Indicates whether the issue has been cleared \\
Ryan Note          & Long text & Optional & Will correct address after lunch. & Optional owner-side note \\
Created By Run     & Text      & Optional & run\_2026\_04\_06\_01 & Useful for traceability \\
Job Number         & Text / linked job & Optional & SM8-10452 & Use when the issue is tied to one job \\
Route Day          & Text      & Optional & Tuesday & Use when the issue is tied to a candidate route-day \\
Route Slot         & Text      & Optional & Van~4 & Use when the issue is tied to a candidate route slot \\
What Needs Fixing  & Long text & Optional & Address missing geocodeable street number. & Plain-language fix request \\
AI Suggested Action & Long text & Optional & Correct address and rerun the week. & Optional action guidance \\
\bottomrule
\end{longtable}
}

\subsection*{Route Communications}

Used in the post-sync route/day/technician interface for route-specific issues such as replacement results, booking-window-sensitive notes, or weak-but-workable route warnings (addendum~\S4.2).
Route Day and Route Slot are \textbf{required} in the post-sync context.

\medskip
{\small
\begin{longtable}{lclp{2.8cm}p{3.2cm}}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Req?} & \textbf{Example} & \textbf{Notes} \\
\midrule
Communication ID   & Text / autonumber & Required & COM-00127 & Unique row identifier \\
Communication Type & Single select    & Required & Replacement Result & Examples differ slightly by table \\
Severity           & Single select    & Required & Warning & Info / Warning / Action Needed \\
Schedule Week      & Linked record    & Required & Week of 2026-04-06 & Parent review record \\
Message            & Long text & Required & Job 10631 replaced: best available but geography is weak. & Human-readable message Ryan can scan quickly \\
Resolved           & Checkbox  & Required & FALSE & Indicates whether the issue has been cleared \\
Ryan Note          & Long text & Optional & Will correct address after lunch. & Optional owner-side note \\
Created By Run     & Text      & Optional & run\_2026\_04\_06\_01 & Useful for traceability \\
Route Day          & Text      & Required & Thursday & Route context is required in the post-sync interface \\
Route Slot         & Text      & Required & Chris & Technician or Van~4 row for the affected route \\
Job Number         & Text / linked job & Optional & SM8-10631 & Use when the issue ties to one job \\
Affected Window    & Text      & Optional & 7-8 first slot & Use when the note involves booking-window feasibility \\
Outside Normal Parameters & Checkbox & Optional & TRUE & Flags weak-but-workable outcomes \\
\bottomrule
\end{longtable}
}

Both communication tables are implemented as one row per issue, not aggregated summary blobs.

\subsection*{Internal Event to Communication Mapping}

The pipeline converts internal \texttt{Exclusion} and \texttt{ReviewFlag} objects into Communication records using the mapping below.
The \texttt{generate\_pre\_route\_comms} and \texttt{generate\_route\_comms} functions in \texttt{nes\_dispatch/postprocess/communications.py} implement this mapping.

\medskip
{\small
\begin{longtable}{lllp{4.5cm}}
\toprule
\textbf{Internal Code} & \textbf{Target Table} & \textbf{Comm.\ Type} & \textbf{Severity} \\
\midrule
\multicolumn{4}{l}{\emph{Exclusion reason codes (one Pre-Route record per excluded job):}} \\
\texttt{QUEUE\_URGENT}       & Pre-Route & Queue Exclusion      & Info \\
\texttt{QUEUE\_ON\_HOLD}     & Pre-Route & Queue Exclusion      & Info \\
\texttt{MISSING\_GEOCODE}    & Pre-Route & Skipped Job          & Action Needed \\
\texttt{INVALID\_CATEGORY}   & Pre-Route & Skipped Job          & Action Needed \\
\texttt{NO\_ELIGIBLE\_TRIPLE}& Pre-Route & Capacity Shortfall   & Warning \\
\texttt{SKILL\_MISMATCH}     & Pre-Route & Capacity Shortfall   & Warning \\
\texttt{VEHICLE\_MISMATCH}   & Pre-Route & Capacity Shortfall   & Warning \\
\texttt{V4\_CATEGORY\_BLOCKED}& Pre-Route & Vehicle Restriction & Warning \\
\texttt{BOOKED\_CAP\_FULL}   & Pre-Route & Capacity Shortfall   & Warning \\
\texttt{PROHIBITED\_PAIR}    & Pre-Route & Scheduling Conflict  & Warning \\
\texttt{CLUSTER\_RADIUS}     & Pre-Route & Geography Issue      & Warning \\
\texttt{ROUTE\_DROP}         & Pre-Route & Route Drop           & Warning \\
\midrule
\multicolumn{4}{l}{\emph{Review-flag codes (week-level $\to$ Pre-Route; route-level $\to$ Route):}} \\
\texttt{ROUTE\_DROP}         & Route     & Route Drop           & Warning \\
\texttt{DUP\_ADDR}           & Pre-Route\textsuperscript{*} & Duplicate Review     & Warning \\
\texttt{GEOCODE\_OOB}        & Pre-Route & Skipped Job          & Warning \\
\texttt{WEAK\_STANDBY}       & Pre-Route & Weak Route           & Warning \\
\texttt{MISSING\_PLANNED\_HOURS} & Pre-Route & Missing Planned Hours & Warning \\
\texttt{TECH\_OVERLOAD}      & Pre-Route & Capacity Shortfall   & Warning \\
\texttt{VEH\_BOTTLENECK}     & Pre-Route & Capacity Shortfall   & Warning \\
\texttt{HELPER\_REQUIRED}    & Route     & Helper Note          & Info \\
\texttt{CROSS\_AREA}         & Route     & Geography Issue      & Warning \\
\texttt{NO\_FEASIBLE}        & Route     & Weak Route           & Warning \\
\bottomrule
\end{longtable}
}

\noindent\textsuperscript{*}\texttt{DUP\_ADDR} is routed to Pre-Route by default.
Per spec~\S8.1, if a suspected duplicate ``materially affects route construction,'' it may also surface as a Route Communication tied to the affected route/day.

% ===================================================================
\section*{Callable Workflows}
\label{sec:workflows}
% ===================================================================

Initial Build defines exactly three callable workflows:

\begin{enumerate}
    \item \textbf{Weekly Run.} Operates from the Schedule Week context.  Produces the proposed week, pre-route maps (one per day/route), and any needed Pre-Route Communications.
    \item \textbf{Replacement Job.} Operates from the post-sync single-route / single-day / single-technician view.  Returns up to two unranked replacement candidates using current route context.
    \item \textbf{Replacement Route.} Operates from the same post-sync route view.  Rebuilds only that one day for that same technician/route, including standby jobs.
\end{enumerate}

A generic freeform communication button is out of scope for Initial Build unless testing later proves it is needed.

% ===================================================================
\section*{Transcription Calendar Output}
\label{sec:transcription}
% ===================================================================

The transcription-calendar output is a simple week-view grid for Ryan's review and manual transcription.
It is \emph{not} a timed appointment calendar and not a direct ServiceM8 write-back.

\begin{itemize}
    \item 5 columns: Monday--Friday, each labelled with the date.
    \item 4 route rows: Technician~1, Technician~2, Technician~3, Van~4.
    \item Each cell contains \textbf{job numbers only}.
    \item Customer names, appointment times, and narrative notes do \emph{not} appear in calendar cells.
    \item Supporting flags, weak-route notes, and explanations belong in the review outputs or communication records.
\end{itemize}

Example layout (week of 4/6):

\medskip
{\small
\begin{longtable}{lccccc}
\toprule
\textbf{Route slot} & \textbf{Mon 4/6} & \textbf{Tue 4/7} & \textbf{Wed 4/8} & \textbf{Thu 4/9} & \textbf{Fri 4/10} \\
\midrule
Technician 1 & 10452, 10631 & 10510 & 10677, 10690 & 10588 & 10491 \\
Technician 2 & 20311 & 20314, 20319 & & 20341 & 20356 \\
Technician 3 & & 30402 & 30405, 30411 & 30440 & 30451 \\
Van 4        & 40118 & & 40127 & & 40135 \\
\bottomrule
\end{longtable}
}

This grid may be stored as Airtable fields, a rendered attachment, or another Airtable-friendly artefact, so long as it preserves the weekday/date column structure and route-row layout.

\paragraph{Post-approval transcription result.}
The spec contract (\S2) requires the engine to define what Python delivers after Ryan approves.
Once the Schedule Week is approved, the same transcription-calendar data and route groupings become the \textbf{post-approval transcription result}---the final artefact Ryan uses for manual ServiceM8 entry.
Python does not own post-approval calendar population (that is Amy\,/\,Airtable's responsibility per \S10); Python's obligation is to produce the data in a form that Airtable can hand off to Ryan's downstream workflow.

% ===================================================================
\section*{Approve / Reject / Re-run Workflow}
\label{sec:approve}
% ===================================================================

On the Schedule Week record, Initial Build supports:
\begin{itemize}
    \item \textbf{Approve} -- accept the proposed week for transcription.
    \item \textbf{Reject / Re-run} -- captures 1--3 short reason codes plus an optional short note, then reruns the same week using the same weekly parameters and exceptions.
\end{itemize}

Suggested Initial Build reason codes:
\begin{enumerate}
    \item Geography / route shape issue.
    \item Wrong job selection / missed important job.
    \item Wrong capacity / route load.
    \item Wrong vehicle / helper / radiator logic.
    \item Other.
\end{enumerate}

The five pre-route maps should be delivered on the Schedule Week record in the first interface, before ServiceM8 transcription.

% ===================================================================
\section*{Ownership Boundaries}
\label{sec:ownership}
% ===================================================================

This section exists to prevent scope confusion (spec \S10).
Some functions belong to the Python engine (``Freelancer''), some belong to Airtable\,/\,Amy, some are shared-interface items, and some still have an unresolved owner.
The freelancer should not assume missing ownership means missing importance.

\medskip
{\small
\begin{longtable}{p{4.5cm}p{4.5cm}p{4.5cm}}
\toprule
\textbf{Python Engine (Freelancer) Owns} & \textbf{Amy / Airtable Owns} & \textbf{Shared Interface} \\
\midrule
Weekly engine logic; route selection; geography logic; aging/fairness application; holiday modifiers; helper-required flags; route maps; draft schedule outputs; replacement/day-recovery recommendation logic. &
Airtable display/storage; dashboard/workflow UX; queue/status automations; Postmark/email setup; ServiceM8 note-writing and message generation; Ryan-facing exception entry; holiday/company-wide exception support; post-approval calendar population; communications surfaces in Airtable; radiator-hours table; operational interface layer around the engine. &
Input/export schemas; review handoff; replacement/rebuild triggers/buttons; any Area Readiness storage/display split; Run AI Analysis trigger; data handoff into and out of the engine; Schedule Week review outputs; Pre-Route and Route Communications; post-approval handoff. \\
\bottomrule
\end{longtable}
}

\paragraph{Initial build communication model (\S10.1).}
Initial build uses a minimal, record-based communication model rather than a broad chat system.
Brain-to-Ryan communication surfaces through Pre-Route Communications (before transcription) and Route Communications (post-sync route interface).
Rare Ryan-to-Brain actions happen through \textbf{four standard buttons} tied to the correct context:
\begin{itemize}
    \item \textbf{Run AI Analysis} and \textbf{Reject / Re-run} on the Schedule Week record.
    \item \textbf{Replacement Job} and \textbf{Replacement Route} on the post-sync route view.
\end{itemize}
A generic freeform communication button is out of scope for Initial Build unless testing later proves it is needed.
The Airtable trigger/button design for these actions is Amy-side/shared-interface work, not something the Python engine should silently assume exists.

\paragraph{Shared interface.}
Shared-interface items are capabilities that depend on both sides even if one side ultimately executes more of the work.
These include the Run AI Analysis trigger, data handoff into and out of the engine, replacement/rebuild triggers, and any post-approval handoff needed to move approved schedule results into Ryan's downstream workflow.

\paragraph{TBD owner.}
Some items are required system capabilities without a final owner.
The most important: the \textbf{exception interaction channel}---a limited back-and-forth for rare cases where AI cannot safely proceed alone (replacement-job approval, accepted-window clarification, first-only/last-only replacement questions).
Normal weekly scheduling remains mostly one-directional.
The exact implementation method is not yet defined---it may be built in Airtable, in an AI wrapper/interface layer, by the freelancer, or as shared work.
For Initial Build, treat this as a required system capability with implementation owner TBD.

\paragraph{Area Readiness ownership note (\S10.2).}
Area Readiness remains a gray area and should not be over-specified.
Current best assumption: if Airtable can calculate it, Airtable should calculate, store, and display it.
The engine does not need to own Area Readiness calculation in Initial Build unless later directed otherwise.

% ===================================================================
\section*{Post-Route Update Rule}
% ===================================================================

After a weekly plan is finalised and approved, assigned jobs are marked as \texttt{scheduled} and removed from the candidate pool for subsequent planning iterations.

% ===================================================================
\section*{Route Map Rendering}
\label{sec:mapping}
% ===================================================================

The engine generates a per-route PNG map for every (technician, vehicle, day) bundle.
These maps are the primary visual review artefact: they let the owner confirm geographic sanity at a glance without reading coordinate tables.

\subsection*{Implementation}

Map rendering is implemented in \texttt{nes\_dispatch/mapping.py} using OSMnx, NetworkX, and Matplotlib.
For each route the pipeline:

\begin{enumerate}
    \item Collects all stop coordinates (depot + job sites) and computes the geographic centre and bounding radius.
    \item Downloads a road-network graph from OpenStreetMap via \texttt{osmnx.graph\_from\_point(centre, dist=radius)}. Per-route graphs are small (typically 500--2\,000 nodes) and download in seconds.
    \item Snaps each coordinate to the nearest road-network node.
    \item Computes road-following paths between consecutive stops via shortest-path search on the road graph.
    \item Renders the map with Matplotlib: light-gray road network on a dark background, coloured route segments, a red square for the depot, and green circles for job stops.
\end{enumerate}

OSMnx disk caching is enabled so that repeated runs for the same service area do not re-download road data.
A retry mechanism with exponential backoff handles transient Overpass API failures.

\subsection*{Example Route Maps}

Figures~\ref{fig:route_wed}--\ref{fig:route_fri} show three route maps generated by the engine from the example dataset.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.75\textwidth]{figures/route_wed_example.png}
    \caption{Wednesday route for Technician~T-01 with Vehicle~V-1. The red path follows the road network from the depot (red square) to a distant job stop (green circle), illustrating a longer-distance service call through rural New Hampshire.}
    \label{fig:route_wed}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.75\textwidth]{figures/route_thu_example.png}
    \caption{Thursday route for Technician~T-01 with Vehicle~V-3. The route runs from the depot through a suburban area to a job stop in the southern section, showing the road-following path along major corridors.}
    \label{fig:route_thu}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.75\textwidth]{figures/route_fri_example.png}
    \caption{Friday route for Technician~T-02 with Vehicle~V-4 in an urban setting. Two job stops are visible with the route path following the city street grid between them.}
    \label{fig:route_fri}
\end{figure}

% ===================================================================
\section*{Output Artefacts}
% ===================================================================

Each weekly run produces:
\begin{itemize}
    \item \textbf{Transcription calendar} -- 5-column weekday grid $\times$ 4 route rows with job numbers only (Section~\ref{sec:transcription}).
    \item \textbf{Route grouping output} -- assigned jobs, standby jobs, and anchor-hold jobs shown distinctly.
    \item \textbf{Route flags} -- helper required = yes/no; first-only / last-only notes; odd-route explanation layer.
    \item \textbf{Route notes} -- explain why a strange-looking route still makes sense (e.g.\ ``Priority job SM8-10677 justified geography weakening; route remains workable via Providence corridor'').  Route notes are a spec~\S1 in-scope deliverable, distinct from route flags.
    \item \textbf{Day/route maps} -- job pins labelled with Job~\#, job category, and planned hours; vehicle colour coding; route notes overlaid.  Five maps (one per day) delivered on the Schedule Week record before transcription.
    \item \textbf{Pre-Route Communications} -- one record per week-build issue, surfaced before ServiceM8 transcription (Section~\ref{sec:comms}).
    \item \textbf{Route Communications} -- one record per route-specific issue, surfaced in the post-sync route/day/technician interface (Section~\ref{sec:comms}).
    \item \textbf{Area report / readiness} -- communication-support reporting (optional / pending Amy).
    \item \textbf{Reason codes} -- traceable to the rules that produced each decision.  Exclusion reasons for unassigned jobs are delivered as Pre-Route Communication records (spec~\S14.2), not a separate report.
\end{itemize}

Outputs are designed for human review by Ryan; the engine does \emph{not} auto-book jobs.

\subsection*{Proposed Filename Conventions}

The spec proposes the following file artefacts (exact names may change at implementation lock):

\medskip
{\small
\begin{longtable}{llp{5.5cm}l}
\toprule
\textbf{Artefact} & \textbf{Type} & \textbf{Purpose} & \textbf{Status} \\
\midrule
\multicolumn{4}{l}{\textbf{Inputs (4 payloads + lookup tables)}} \\
\texttt{jobs\_input.csv}              & Input  & Candidate Jobs --- weekly exported eligible-job dataset. & Locked \\
\texttt{weekly\_context.csv}          & Input  & Weekly Context --- week-of, season, include-V-4, helpers, holidays. & Locked \\
\texttt{weekly\_exceptions.csv}       & Input  & Weekly Exceptions --- technician-day blocks, vehicle unavailability. & Locked \\
\texttt{area\_rules.csv}              & Input  & Lookup / Rules --- area naming, adjacency, grouping rules. & Locked \\
\texttt{technicians.csv}              & Input  & Lookup / Rules --- technician roster (ID, name, depot). & Locked \\
\texttt{vehicles.csv}                 & Input  & Lookup / Rules --- vehicle roster (ID, capability tags, capacity). & Locked \\
\midrule
\multicolumn{4}{l}{\textbf{Outputs (Schedule Week artefacts)}} \\
\texttt{transcription\_calendar.\{ext\}} & Output & Transcription-calendar grid (5$\times$4, job numbers only). The primary deliverable. & Required \\
\texttt{route\_grouping.json}         & Output & Route groupings with assigned, standby, and anchor-hold lists. & Required \\
\texttt{pre\_route\_comms.json}       & Output & Pre-Route Communication records (one per week-build issue). & Required \\
\texttt{route\_comms.json}            & Output & Route Communication records (one per route-specific issue). & Required \\
\texttt{maps/}                        & Output & Day/route review maps with labelled job pins and route notes. 5 maps. & Required \\
\texttt{area\_report.csv}             & Output & Area-level readiness report (optional / pending Amy). & Provisional \\
\bottomrule
\end{longtable}
}

\noindent\textbf{Spec Table~4 cross-reference.}
The consolidated spec (\S14) proposed \texttt{capacity\_input.csv} (provisional) as a single capacity file; this document splits it into \texttt{weekly\_context.csv} and \texttt{weekly\_exceptions.csv}, both now Locked.
The spec also proposed \texttt{draft\_schedule.html} (provisional output); this document replaces it with \texttt{transcription\_calendar.\{ext\}} to match the transcription-calendar concept.

% ===================================================================
\section*{Adjustable Settings}
% ===================================================================

The following settings can be adjusted between runs to steer planning behaviour without modifying code:

\begin{itemize}
    \item \textbf{Daily time budget} -- caps total travel-plus-service time per route.
    \item \textbf{Max stops per route and per-vehicle capacities} -- global and per-vehicle upper bounds on stop count.
    \item \textbf{Cluster radius and inter-stop limit} -- how far from a depot jobs may be included, and the maximum gap between consecutive stops.
    \item \textbf{Season} -- supplied in Weekly Context (Winter or Summer); controls the scoring tier order and tiebreaker emphasis (aging vs.\ geography).
    \item \textbf{Weekly exceptions} -- technician-day blocks and vehicle unavailability, entered each week before the run.
    \item \textbf{Special-route reservations} -- which jobs require Radiator Run (V-2), NH Overnight, or helper-needed handling.
    \item \textbf{Prohibited pairings and area definitions} -- technician--job exclusions and the geographic area map, maintained in Airtable.
\end{itemize}

Numerical thresholds (time budget, radii, capacity limits) live in a version-controlled config file (JSON/YAML).
Operational data that changes weekly (rosters, exceptions, pairings, area maps) lives in Airtable and is pulled at the start of each run.

% ===================================================================
\section*{Risks and Ambiguities}
% ===================================================================

\paragraph{Phase-1 / Phase-2 feasibility gap.}
Phase~1 budgets planned hours using routing-service travel times, but real-world conditions may differ.
A conservative time-budget buffer (e.g.\ 80\,\% of the true day length) should be used until an iterative feedback loop is added.

\paragraph{Drive-time accuracy.}
Travel times are computed at runtime via a routing service (e.g.\ Google Maps).
If the service returns inaccurate results for rural New England roads, route durations may be mis-estimated.

\paragraph{Greedy scheduling quality.}
The decision-ladder is deterministic but greedy: it processes jobs in ranked order and cannot backtrack.
A high-tier job may consume a slot that would have been better used by multiple lower-tier jobs.
Monitoring unassigned-job counts and fill-rate variance will reveal whether improvements are needed.

\paragraph{Geocoding accuracy.}
Preferred source is Airtable-owned coordinates; Python geocodes as fallback.
Incorrect or ambiguous addresses produce wrong cluster assignments and distance estimates.
Jobs failing geocoding are skipped with a communication record.

\paragraph{Exception staleness.}
Weekly exceptions must be entered before the planning run.
Late-arriving changes (same-day sick leave) are not captured and require manual adjustment or a re-run.

\paragraph{Single-week planning horizon.}
Each run plans one week in isolation.
Jobs deferred this week re-enter the pool next week with higher aging, but there is no multi-week look-ahead to prevent systematic starvation.

\paragraph{Radiator-hours tuning.}
Ryan still needs to set and tune the first real values in the Airtable radiator-hours table.
Until those are calibrated, radiator route timing may be inaccurate.

\paragraph{Superseded prior assumptions.}
\begin{itemize}
    \item Current status is not a required engine input.
    \item The engine talks in terms of assigned jobs, standby jobs, and anchor-hold jobs---not vague ``backup jobs.''
    \item Scheduling Preferences are not the same as the rebook-age rule.
    \item Vehicle~4 is not a full equivalent route slot; it is a lighter overflow route with work-type restrictions.
    \item Pre-existing calendar load is represented through Weekly Exceptions, not live ServiceM8 calendar access.
\end{itemize}

% ===================================================================
\section*{Engine File Layout}
\label{sec:engine_structure}
% ===================================================================

\begin{verbatim}
nes_dispatch/
    __init__.py
    data/
        loaders.py          # CSV / Airtable readers -> dataclasses
        models.py           # Job, Technician, Vehicle, WeeklyException, etc.
        validators.py       # Pre-run sanity checks (coordinates, missing fields)
    rules/
        scoring.py          # Decision-ladder ranking
        eligibility.py      # 12 named eligibility rules -> pass/fail per slot
        slot_selection.py   # Best-fit ranking (closest depot, lightest day)
    routing/
        distance.py         # Haversine distance computation
        nearest_neighbour.py# NN stop ordering + feasibility verification
    postprocess/
        standby.py          # Standby job ranking
        readiness.py        # Area readiness computation
        flags.py            # Review flags (10 codes, 3 severity levels)
        communications.py   # Exclusion/flag -> Communication record conversion
    mapping.py              # Route map rendering (OSMnx + Matplotlib)
    pipeline.py             # Top-level orchestrator (the 9-step pipeline)
\end{verbatim}

Each stage is a function that takes typed dataclass inputs and returns typed outputs.
The pipeline is fully deterministic---same inputs always produce the same plan.
Unit tests cover individual stages with small synthetic fixtures; an integration test runs the full pipeline on the example CSVs.

% ===================================================================
\section*{Code vs.\ Configuration vs.\ Airtable Split}
\label{sec:split}
% ===================================================================

Not all system behaviour belongs in code.
The following table defines which concerns live in Python source, which in a version-controlled config file (JSON/YAML), and which in Airtable (editable by the owner at any time).

\medskip
{\small
\begin{longtable}{p{5.2cm}ccc}
\toprule
\textbf{Concern} & \textbf{Code} & \textbf{Config} & \textbf{Airtable} \\
\midrule
Rule-based scheduling logic                   & $\bullet$ &           &           \\
Nearest-neighbour routing                     & $\bullet$ &           &           \\
Eligibility-check rules                       & $\bullet$ &           &           \\
Distance computation                          & $\bullet$ &           &           \\
Output rendering (maps, text)                 & $\bullet$ &           &           \\
\midrule
Daily time budget                             &           & $\bullet$ &           \\
Global max stops                              &           & $\bullet$ &           \\
Cluster radius                                &           & $\bullet$ &           \\
Inter-stop radius                             &           & $\bullet$ &           \\
Seasonal tier/tiebreaker settings             &           & $\bullet$ &           \\
Workload-review thresholds                    &           & $\bullet$ &           \\
\midrule
Vehicle roster \& capability tags             &           &           & $\bullet$ \\
Vehicle availability days                     &           &           & $\bullet$ \\
Vehicle capacity overrides                    &           &           & $\bullet$ \\
Technician roster \& skills                   &           &           & $\bullet$ \\
Technician home-depot coordinates             &           &           & $\bullet$ \\
Weekly exceptions (PTO, maintenance)          &           &           & $\bullet$ \\
Geographic area definitions                   &           &           & $\bullet$ \\
Radiator-hours table (Airtable formula)       &           &           & $\bullet$ \\
Job candidate pool (from ServiceM8 sync)      &           &           & $\bullet$ \\
\bottomrule
\end{longtable}
}

\textbf{Code} holds logic that requires developer review to change.
\textbf{Config} holds numerical parameters adjusted between runs; the config file is version-controlled alongside the code.
\textbf{Airtable} holds operational data that changes weekly or on demand; the engine pulls the latest state at the start of each run.

% ===================================================================
\section*{Validation and Testing}
\label{sec:validation}
% ===================================================================

\subsection*{Input Validation (Pre-run)}

\texttt{validators.py} performs the following checks when data is loaded.  If any check fails, the run is aborted with a consolidated error report.

\begin{enumerate}
    \item \textbf{Schema completeness.} Every required CSV column is present and non-null for every row.
    \item \textbf{Coordinate bounds.} All latitudes fall within $[41, 48]$ and longitudes within $[-74, -67]$ (approximate New England bounding box).
    \item \textbf{Referential integrity.} Every exception scope ID matches an existing technician or vehicle ID.  Every area ID on a job exists in the area definitions.
    \item \textbf{Availability consistency.} After applying exceptions, at least one (technician, vehicle, day) triple must remain feasible.
    \item \textbf{Duplicate detection.} Duplicate job, technician, or vehicle IDs are flagged.
\end{enumerate}


\subsection*{Unit and Integration Tests}

Unit tests cover individual stages with small synthetic fixtures:
\begin{itemize}
    \item Scoring: verify decision-ladder tier scores match hand-computed values.
    \item Eligibility: verify each of the 12 rules returns expected pass/fail.
    \item Slot selection: verify best-fit ranking given pre-computed eligibility.
    \item Nearest-neighbour: 5-node instance with known tour; compare output sequence.
    \item Feasibility: route exceeding the time budget; verify job-drop and re-sequence.
    \item Helper: helper-needed job assigned to route; confirm flag is set.
\end{itemize}

An integration test runs the full pipeline on the example CSVs and asserts that at least one route is generated and no validation errors occur.

% ===================================================================
\section*{Review Flags and Exception Outputs}
\label{sec:flags}
% ===================================================================

The engine produces a structured set of review flags alongside every weekly plan.
These flags are designed for the non-technical owner to scan quickly and decide whether to approve, adjust, or re-run.

\subsection*{Flag Categories}

Each flag has a severity (\texttt{INFO}, \texttt{WARN}, \texttt{CRITICAL}), a short code, and a human-readable message.

\medskip
{\small
\begin{longtable}{llp{7.5cm}}
\toprule
\textbf{Code} & \textbf{Severity} & \textbf{Meaning} \\
\midrule
\texttt{DUP\_ADDR}    & INFO     & Two or more jobs share the same address. Grouped for route evaluation; review for possible duplicates. \\
\texttt{CROSS\_AREA}  & INFO     & A route mixes jobs from two or more areas. May be optimal geographically but warrants a glance. \\
\texttt{WEAK\_STANDBY} & WARN    & Fewer than 2 feasible standby jobs exist for a route. If a scheduled job drops, coverage is thin. \\
\texttt{VEH\_BOTTLENECK} & WARN  & A vehicle is assigned at 100\,\% capacity on 4+ days. Little room for ad-hoc or emergency jobs. \\
\texttt{TECH\_OVERLOAD} & WARN   & A technician's total weekly service time exceeds 90\,\% of their available hours. \\
\texttt{MISSING\_PLANNED\_HOURS} & WARN & A job has no planned-hours value; engine substitutes the category average but flags for review. \\
\texttt{HELPER\_REQUIRED} & INFO   & Route contains one or more helper-required jobs.  The entire route/day is flagged as helper-required. \\
\texttt{ROUTE\_DROP}  & WARN     & One or more Phase-1-scheduled jobs were dropped during Phase~2 routing because travel time pushed the route past the daily time budget. \\
\texttt{GEOCODE\_OOB} & CRITICAL & A job's coordinates fall outside the New England bounding box. Likely a geocoding error; the job is excluded and must be corrected. \\
\texttt{NO\_FEASIBLE} & CRITICAL & A (technician, vehicle, day) bundle produced by Phase~1 has no feasible route at all in Phase~2. The entire bundle is unserved. \\
\bottomrule
\end{longtable}
}

\subsection*{Exclusion Reason Codes}

For every job in the weekly candidate pool that is \emph{not} assigned to a route, the engine emits a reason code explaining why.
These codes are delivered as Pre-Route Communication records per spec~\S14.2 (no standalone validation report).

\medskip
{\small
\begin{longtable}{lp{9cm}}
\toprule
\textbf{Reason Code} & \textbf{Explanation} \\
\midrule
\texttt{QUEUE\_URGENT}       & Job is in the Urgent queue---handled outside weekly schedule. \\
\texttt{QUEUE\_ON\_HOLD}     & Job is in the On Hold queue---excluded until re-queued. \\
\texttt{NO\_ELIGIBLE\_TRIPLE}& No (technician, vehicle, day) triple satisfies all 12 eligibility rules. \\
\texttt{CAPACITY\_FULL}      & All eligible slots were already at physical vehicle capacity. \\
\texttt{BOOKED\_CAP\_FULL}   & All eligible slots have already reached the seasonal booking target (winter~4 / summer~3). \\
\texttt{V4\_CATEGORY\_BLOCKED}& Job category is not in V-4's allowed list (Steam System Inspection, Service Call, Boiler Maintenance) and V-4 was the only available vehicle. \\
\texttt{TIME\_BUDGET}        & Including this job would exceed the daily time budget in every eligible slot. \\
\texttt{SKILL\_MISMATCH}     & No available technician has the required skills. \\
\texttt{VEHICLE\_MISMATCH}   & No available vehicle has the required capability tags (e.g.\ radiator job but V-2 unavailable). \\
\texttt{PROHIBITED\_PAIR}    & Every available technician is in the prohibited-pairs set for this job. \\
\texttt{CLUSTER\_RADIUS}     & Job is beyond the cluster radius from every available technician depot. \\
\texttt{ROUTE\_DROP}         & Job was scheduled by Phase~1 but dropped during Phase~2 routing due to travel-time infeasibility. \\
\texttt{MISSING\_GEOCODE}    & Job could not be geocoded; skipped with a communication record. \\
\texttt{INVALID\_CATEGORY}   & Job category not in the 17 recognised categories. \\
\bottomrule
\end{longtable}
}  
\subsection*{Delivery Mechanism}

Per spec~\S14.2: ``No standalone validation report is needed.  Any skipped jobs should be surfaced through the same communication flow using only Job~\# and what needs fixing.''
Accordingly:

\begin{itemize}
    \item \textbf{Exclusion reasons} are delivered as \textbf{Pre-Route Communication} records (one per excluded job), with
          \texttt{Communication Type} and severity mapped per the internal-event table in Section~\ref{sec:comms},
          and the \texttt{What Needs Fixing} field populated with the human-readable detail string.
    \item \textbf{Review flags} are routed to Pre-Route or Route Communications depending on scope:
          week-level flags (e.g.\ \texttt{DUP\_ADDR}, \texttt{WEAK\_STANDBY}, \texttt{MISSING\_PLANNED\_HOURS}) become Pre-Route Communications;
          route-specific flags (e.g.\ \texttt{HELPER\_REQUIRED}, \texttt{CROSS\_AREA}) become Route Communications attached to the affected route/day/slot.
    \item \texttt{CRITICAL}-severity communications (``Action Needed'') are additionally surfaced as bold-red banners at the top of the Schedule Week review, ensuring they are not overlooked.
\end{itemize}

The engine retains structured \texttt{ReviewFlag} and \texttt{Exclusion} objects internally for programmatic access, but the \emph{primary} output channel for Ryan is the communication records described in Section~\ref{sec:comms}.
The conversion is implemented by \texttt{generate\_pre\_route\_comms} and \texttt{generate\_route\_comms} in \texttt{nes\_dispatch/postprocess/communications.py}.

% ===================================================================
\section*{Important Context (Not Direct Engine Logic)}
\label{sec:context}
% ===================================================================

The following items are not engine logic but affect interpretation (spec~\S11):


\begin{itemize}
    \item \textbf{Scheduling Preferences detail source} -- queue is settled, but the exact source of the preference text/details is still pending Amy.
    \item \textbf{Radiator module} -- the Airtable radiator-hours structure is now defined, but Ryan still needs to set and tune the first real values.
    \item \textbf{Pre-Route / Route Communications Airtable structure} -- Python-side concepts are defined (Section~\ref{sec:comms}), but the final Airtable build details still need to be implemented.
    \item \textbf{Revise / Reject tuning} -- a minimal Reject\,/\,Re-run workflow is defined for Initial Build (Section~\ref{sec:approve}), but finer-grained revision behaviour can expand after testing.
    \item \textbf{Area Readiness final scope} -- optional / pending Amy.
    \item \textbf{Generic communication button} -- intentionally deferred unless testing shows standard buttons are not enough.
\end{itemize}

% ===================================================================
\section*{Deferred Items}
% ===================================================================

\begin{itemize}
    \item \textbf{Scheduling Preferences detail source} -- queue placement is settled; the authoritative source of the exact preference text/details is pending Amy.
    \item \textbf{Radiator module tuning} -- broader radiator behaviour and tuning still need testing.  Radiator-hour formulas themselves live in Airtable and must be editable there, not hard-coded in Python.
    \item \textbf{$2\times$ average-wait trigger source} -- Airtable will supply the signal/flag.
    \item \textbf{Area Readiness final scope} -- optional / pending Amy.
    \item \textbf{Exception interaction channel} -- required system capability; implementation owner TBD.
    \item \textbf{Hosting / deployment details} -- technical handoff still needed.
    \item \textbf{Revision-loop schema} -- structured reason codes for the reject/re-run loop are deferred until testing reveals real failure modes.  The five starter codes in the Approve\,/\,Reject section are provisional.
    \item \textbf{\texttt{ROUTE\_SHAPE\_OK} eligibility rule} -- full route-shape analysis (cluster tightness, corridor flow, zigzag detection); currently approximated by depot-to-job haversine distance.
    \item \textbf{\texttt{POSITION\_OK} eligibility rule} -- first-only / last-only enforcement based on accepted booking windows.
    \item \textbf{Geography flexibility per queue} -- differentiated geography tolerance (Normal = tight, Priority/AQ = substantial, RS = moderate); currently uniform proximity.
    \item \textbf{Providence shop as routing factor} -- spec requires Providence shop as an explicit routing node for Providence-assisted route shapes.
    \item \textbf{Booking-window assignment and feasibility flagging} -- window slot assignment (7--8, 8--9, etc.) and route/window conflict detection.
    \item \textbf{Anchor-hold designation} -- marking a high-value job as the route's justification on fragile routes.
    \item \textbf{Replacement Job workflow} -- return up to two unranked replacement candidates using current route context.
    \item \textbf{Replacement Route workflow} -- rebuild one day for the same technician/route, including standby jobs.
    \item \textbf{Real routing service integration} -- spec \S3 requires Google Maps or equivalent for travel-time budgeting; current code uses haversine-based time estimates.
\end{itemize}

% ===================================================================
\section*{Live-Week Testing and Acceptance}
\label{sec:testing_live}
% ===================================================================

Initial Build testing uses the real upcoming live scheduling week, not an elaborate synthetic test programme.

\begin{itemize}
    \item Ryan may start the week as early as Monday.  If the first output is bad, the freelancer may revise within that week, but only up to a practical Thursday deadline.
    \item If the output is still not usable by Thursday, Ryan reverts to manual scheduling for that week and AI tries again on the next live week.
    \item \textbf{Acceptance standard:} most live weeks should be approved by Ryan with no changes.  The goal of the project is that AI does the scheduling work, not Ryan.  One imperfect route does not fail the entire week, but if Ryan is routinely rebuilding, the system has not met the goal.
    \item If two live scheduling weeks fail, the project pauses automatically for review before more implementation continues.
\end{itemize}

% ===================================================================
\section*{Future Considerations (Beyond Initial Build)}
% ===================================================================

\begin{itemize}
    \item \textbf{Iterative Phase-1/Phase-2 feedback:} re-schedule jobs dropped by Phase~2 into alternative slots.
    \item \textbf{Route improvement pass:} after nearest-neighbour sequencing, apply pairwise arc swaps to shorten routes.
    \item \textbf{Exact appointment-time output:} assign specific arrival windows per stop once route ordering is reliable.
    \item \textbf{Live traffic / drive-time API integration:} replace static routing with live data.
    \item \textbf{Historical job-outcome data:} feed completion rates and no-show rates back into the scoring tiers.
    \item \textbf{Multi-week rolling horizon planning:} prevent systematic starvation of low-value areas.
    \item \textbf{Generic freeform communication button:} deferred unless testing shows standard buttons are insufficient.
    \item \textbf{Partial-day exceptions:} extend beyond full technician-day blocks.
    \item \textbf{Named helper assignment:} assign specific helpers rather than just flagging helper-required.
\end{itemize}


\end{document}
